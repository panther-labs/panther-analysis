version: 2

jobs:
  build:
    docker:
      - image: circleci/python:3.7.4
    steps:
        - checkout
        - run:
            name: setup
            command: make venv && source venv/bin/activate && make deps
        - run:
            name: CLI CI
            command: make ci
