AnalysisType: pack
PackID: PantherManaged.MongoDb
Description: Group of all MongoDB detections
DisplayName: "Panther MongoDB Atlas Pack"
PackDefinition:
  IDs:
    - MongoDB.Atlas.ApiKeyCreated
    - MongoDB.2FA.Disabled
    # Globals
    - panther_base_helpers
    - panther_config
    - panther_config_defaults
    - panther_config_overrides
