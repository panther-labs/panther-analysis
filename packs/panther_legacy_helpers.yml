AnalysisType: pack
PackID: PantherManaged.PantherLegacyHelpers
Description: Legacy helper functions and their dependencies for backwards compatibility
PackDefinition:
  IDs:
    - panther_base_helpers
    - panther_oss_helpers
    - panther_default
    - panther_aws_helpers
    - panther_config
    - panther_config_defaults
    - panther_config_overrides
    - panther_ipinfo_helpers
    - panther_lookuptable_helpers
    - panther_base_helpers_old
DisplayName: "Panther Legacy Helpers Pack"
