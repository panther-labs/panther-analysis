AnalysisType: pack
PackID: PantherManaged.ScheduledRules
Description: This pack groups high value scheduled queries and rules
PackDefinition:
  IDs:
    # Queries
    - CloudTrail Password Spraying
    - VPC DNS Tunneling
    # Rules
    - CloudTrail.Password.Spraying
    - VPC.DNS.Tunneling
DisplayName: Panther Scheduled Queries and Rules
