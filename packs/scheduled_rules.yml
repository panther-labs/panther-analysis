AnalysisType: pack
PackID: PantherManaged.ScheduledRules.Core
Description: This pack groups a core set of scheduled queries and rules
PackDefinition:
  IDs:
    # Queries
    - CloudTrail Password Spraying
    - VPC DNS Tunneling
    # Rules
    - CloudTrail.Password.Spraying
    - VPC.DNS.Tunneling
DisplayName: Panther Scheduled Queries and Rules
