AnalysisType: pack
PackID: PantherManaged.Snowflake.Core
Description: This pack groups a core set of snowflake security monitoring scheduled queries and rules
PackDefinition:
  IDs:
    # User Activity - Queries
    - Snowflake.BruteForceByIp
    - Snowflake.BruteForceByUsername
    # User Activity - Rules
    - Snowflake.BruteForce
    - Snowflake.MFALogin
    # Admin Activity - Queries
    - Snowflake.AccountAdminGranted
    - Snowflake.PublicRoleGrant
    # Admin Activity - Rules
    - Snowflake.AdminAssigned
    - Snowflake.AdditionalGrantPublicRole
DisplayName: Panther Snowflake Security Monitoring
