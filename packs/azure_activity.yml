AnalysisType: pack
PackID: PantherManaged.Azure.MonitorActivity
Description: Group of Azure.MonitorActivity detections
PackDefinition:
  IDs:
    - Azure.MonitorActivity.Automation.ServerlessExecution
    - Azure.MonitorActivity.KeyVault.KeyPurged
    - Azure.MonitorActivity.Kubernetes.RoleBindingCreated
    - Azure.MonitorActivity.Network.IPDiscovery
    - Azure.MonitorActivity.Storage.Blob.CPKEncryptionDetected
    - Azure.MonitorActivity.Storage.Blob.Uploaded
    - Azure.MonitorActivity.StorageAccount.BlobSoftDeleteDisabled
    # Correlation Rules
    # Globals used in these detections
    - panther_azureactivity_helpers
DisplayName: "Panther Azure MonitorActivity Pack"
